services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:5173" # Mapear 3000 (host) a 5173 (contenedor)
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm run start # Solo Vit